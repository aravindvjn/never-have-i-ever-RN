workflows:
  android:
    name: Android Build
    instance_type: mac_mini  # Corrected instance type
    commands:
      - yarn install
      - npx react-native run-android --variant=release
    artifacts:
      - build/app/outputs/apk/release/app-release.apk
